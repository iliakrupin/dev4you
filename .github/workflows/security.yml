name: Security Checks

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  phase1:
    uses: iliakrupin/home-devsecops/.github/workflows/phase1-hygiene.yml@v1.1.0
    with:
      enforcement: warn
